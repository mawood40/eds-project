[project]
name = "eds-project"
version = "0.1.0"
description = "Electricity dataset preprocessing and PyTorch neural network"
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.0.0",
    "scikit-learn>=1.3.0",
    "torch>=2.0.0",
]
